---
description: Run the project's test suite and summarize failures.
---

Detect and run the project's test command (e.g. `npm test`, `pytest`, `mvn test`, `cargo test`).

Then:
1. Summarize pass/fail counts.
2. For each failure, show the failing assertion and a likely cause.
3. Suggest a fix without applying it unless asked.
