{
  "mcpServers": {
    "db-tools": {
      "command": "node",
      "args": [
        "${CLAUDE_PLUGIN_ROOT}/servers/index.js"
      ],
      "env": {
        "DATABASE_URL": "${DATABASE_URL}"
      }
    }
  }
}
